const express = require('express');
const router = express.Router();

// Common dashboard route (to be implemented)
router.get('/dashboard', (req, res) => {
  // Implement common dashboard logic here
});

module.exports = router;
